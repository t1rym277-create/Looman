# RY-L1390 — GitHub Pages Flat Pack

Upload **all files in this folder** directly to the repository root. No `assets` folder is needed.

Required structure:

- `index.html`
- `01_base_chassis.glb` … `20_fastener_kit.glb`
- `RY-L1390_ASSEMBLED.glb`

Open the GitHub Pages URL after enabling Pages from `main` / root.

The viewer loads the original separate GLB component files directly from the repository root.
