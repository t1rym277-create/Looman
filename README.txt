ГРАНЬ v15 — исправленная remote 3D версия.

Исправления:
- Three.js и GLTFLoader подключаются как ES modules, а не через устаревший THREE.GLTFLoader.
- Poly Haven /files используется как источник прямых URL.
- URL GLTF/GLB ищется рекурсивно в текущем дереве API, без предположения о фиксированной структуре JSON.
- Модели не встроены в HTML.
- Никаких base64 GLB.
- Реальные модели скачиваются при запуске.

Источник ассетов: Poly Haven, CC0.
Нужен интернет. Браузер должен разрешать внешние HTTPS/CDN/CORS запросы.
